<TableContainer component={Paper}>
      <Table sx={{ minWidth: 650 }} aria-label="simple table">
        <TableHead>
          <TableRow>
            <TableCell></TableCell>
            <TableCell align="right">Name</TableCell>
            <TableCell align="right">Age</TableCell>
            <TableCell align="right">Course</TableCell>
            <TableCell align="right">Batch</TableCell>
            <TableCell align="right">Change</TableCell>
          </TableRow>
        </TableHead>
        <TableBody>
          {this.rows.map((row) => (
            <TableRow
              key={row.name}
              sx={{ '&:last-child td, &:last-child th': { border: 0 } }}
            >
              <TableCell component="th" scope="row">
                {row.name}
              </TableCell>
              <TableCell>{this.row.age}</TableCell>
              <TableCell>{this.row.course}</TableCell>
              <TableCell>{this.row.batch}</TableCell>
              <TableCell><button onClick={()=>this.editPage()}>Edit</button></TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </TableContainer>






    import Table from '@mui/material/Table';
import TableBody from '@mui/material/TableBody';
import TableCell from '@mui/material/TableCell';
import TableContainer from '@mui/material/TableContainer';
import TableHead from '@mui/material/TableHead';
import TableRow from '@mui/material/TableRow';
import Paper from '@mui/material/Paper';
import Edit from './Edit';




createData(name, age, course, batch, change) {
    return { name, age, course, batch,change };
  }



  
  rows = [
    this.createData('John','27','MERN','September'),
    this.createData('','','','',''),
    this.createData(),
    this.createData(),
    this.createData(),
  ];